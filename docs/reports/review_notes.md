# Private Review Notes

These notes are intentionally separate from the submission report.

Suggested questions for a technical review:

How does Beacon decide which tables to include before SQL generation?

What is the difference between question signals, metadata grounding, and vector hits?

Why is SQL validation done before execution, and what does it block?

What happens when generated SQL references a valid table outside selected context?

Why does the project keep feedback examples as candidates instead of auto-promoting them?

Which papers justify schema retrieval versus prompt organization versus retry review?
